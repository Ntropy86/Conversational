const fs=require('fs');const path=require('path');const reportPath=path.resolve(__dirname,'frontend/.next/report.html');const s=fs.readFileSync(reportPath,'utf8');const key='setModules(';let idx=s.indexOf(key);if(idx===-1){console.error('setModules not found');process.exit(2);}const before=Math.max(0,idx-2000);const after=Math.min(s.length,idx+2000);console.log('--- context snippet ---');console.log(s.slice(before,after));console.log('--- end snippet ---');