const fs=require('fs');const path=require('path');const reportPath=path.resolve(__dirname,'frontend/.next/report.html');const s=fs.readFileSync(reportPath,'utf8');const key='setModules(';let idx=s.indexOf(key);let count=0;while(idx!==-1){count++;console.log('occurrence',count,'at',idx);const before=Math.max(0,idx-200);const after=Math.min(s.length,idx+200);console.log(s.slice(before,after).replace(/\n/g,' '));idx=s.indexOf(key,idx+1);}console.log('total',count);